info:
  name: Create order with text
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/orders"
  body:
    type: text
    data: keyboard
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
