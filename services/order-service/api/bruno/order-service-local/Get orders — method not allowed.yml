info:
  name: Get orders — method not allowed
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/orders"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "405"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
